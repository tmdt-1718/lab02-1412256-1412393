<div class="message-thumbnail">
	<div class="card-body">
			<div class="col-md-10">
				<h4 class="card-title">
					<%= link_to user_path(a.id), class: "card-title" do %>
						<%= a.email %>
					<% end %>
					</h4>
				<h6 class="card-subtitle mb-2 text-muted">Name: <%= a.name %></h6>
			</div>
  </div>
</div>
